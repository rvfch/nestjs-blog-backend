export const ARTICLE_REMOVED_MESSAGE = (articleId: string) =>
  `Article with id ${articleId} has been removed`;

export const ARTICLE_NOT_FOUND = (articleId: string) =>
  `Article with id ${articleId} not found`;
